module.exports = function (db, {proffyValue, classValue, classScheduleValue}){
    
    // inserir dados na tabela de teachers
    
    // aguarda a conexão antes de executar as proximas linhas
    await db.run()
};



 